# lovelace_gen

#################################################
#
#         HEADER
#
#################################################
type: custom:mod-card
style:
  .: |
    :host {
      grid-area: header !important;
      grid-column-start: span 4 !important;
      margin: 0 !important;
    }

card:
  type: vertical-stack
  cards:
    #################################################
    #     DEFAULT
    #################################################
    # KEEP THIS HERE!!! See dummy button card why...
    - !include dummy_button/dummy_button.card.yaml
    - !include main_menu/main_menu.llg.yaml
    {% if greeting is defined %}
    - type: markdown
      content: |
        {% if greeting.title %}
        # {{ greeting.title }}
        {% endif %}

        {% if greeting.subtitle is defined %}
        ## {{ greeting.subtitle }}
        {% endif %}
    {# - !include
      - greeting/greeting.llg.yaml
      - content: {{ greeting }} #}
    {% endif %}

#################################################
#
#         CONTACT_WINDOW
#
#################################################
contact_window:
  template:
    - base_binary
  icon: |
    [[[ 
      return entity === undefined || entity.state === 'off'
        ? `mdi:window-closed-variant`
        : `mdi:window-open-variant`;
    ]]]
